<Window x:Class="YouTubeDownloader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:YouTubeDownloader.ViewModels"
        mc:Ignorable="d"
        Title="Rajehtube downloader" Height="700" Width="1000"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Input -->
            <RowDefinition Height="Auto"/> <!-- Settings -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Status -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="YouTube" Foreground="{StaticResource AccentBrush}" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text=" Downloader" Foreground="{StaticResource TextBrush}" FontSize="28" FontWeight="Bold"/>
        </StackPanel>

        <!-- Input Area -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Label Content="YouTube URL (Video or Playlist)" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                <TextBox Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}"
                         Height="40" FontSize="16"
                         Padding="12,8"/>
            </StackPanel>
            <Button Grid.Column="1" Command="{Binding FetchCommand}" 
                    Content="FETCH METADATA" Style="{StaticResource ModernButton}" 
                    Width="150" Height="40" Margin="10,22,0,0"/>
        </Grid>

        <!-- Settings Area -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Margin="0,0,20,0">
                <Label Content="Quality / Format" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                <ComboBox ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}"
                          Style="{StaticResource ModernComboBox}"
                          Height="35" VerticalContentAlignment="Center">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Save Path" Foreground="{StaticResource MutedTextBrush}" FontSize="12"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Text="{Binding DownloadFolder}" IsReadOnly="True" 
                             Style="{StaticResource ModernTextBox}" Height="35"/>
                    <Button Grid.Column="1" Command="{Binding BrowseFolderCommand}"
                            Content="..." Style="{StaticResource SecondaryButton}" 
                            Width="40" Height="35" Margin="5,0,0,0"/>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="20,0,0,0">
                <Button Command="{Binding DownloadCommand}" Content="DOWNLOAD SELECTED" 
                        Style="{StaticResource ModernButton}" Height="35" Width="180"/>
                <Button Command="{Binding CancelCommand}" Content="CANCEL" 
                        Style="{StaticResource SecondaryButton}" Height="35" Width="80" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Content Area -->
        <Grid Grid.Row="3">
            <DataGrid ItemsSource="{Binding Videos}" Style="{StaticResource ModernDataGrid}"
                      SelectionMode="Extended">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="Sel" Binding="{Binding IsSelected}" Width="40" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="80" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Progress" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Margin="5,0">
                                    <ProgressBar Value="{Binding Progress}" Style="{StaticResource ModernProgressBar}"/>
                                    <TextBlock Text="{Binding Progress, StringFormat={}{0:0}%}" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                                               FontSize="10" Foreground="{StaticResource TextBrush}"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="150" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Status Area -->
        <Border Grid.Row="4" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}" Margin="0,10,0,0" Padding="0,5,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Status}" Foreground="{StaticResource MutedTextBrush}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Overall Progress:" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <ProgressBar Value="{Binding OverallProgress}" Style="{StaticResource ModernProgressBar}" Width="200" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</Window>
